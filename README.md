# ml-waf-proxy
A Web Application Firewall (WAF) Proxy sits between clients and your backend. Unlike traditional WAFs that rely solely on static regex rules, this system augments rule-based detection with machine learning to catch unknown, obfuscated, or evolving attacks while keeping latency low.
